// Shared UI primitives for the Doctium mobile apps (patient + doctor). Both apps
// previously kept byte-identical copies of these; this is the single source.
export * from "./theme";

// Self-contained common components (theme + expo libs only).
export * from "./components/common/Avatar";
export * from "./components/common/Input";
export * from "./components/common/StarRating";
